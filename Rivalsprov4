--[[
	WARNING: Heads up! This script has not been verified by ScriptBlox. Use at your own risk!
]]

-- Rivals Script - Rayfield UI Edition (Improved & Fixed)
-- Based on your pasted script. Fixed: duplicate flags, aimbot lag/scope bugs, infinite jump logic, speed hack spam, raycast safety, added missing toggles (Target NPCs, Head Size), modern raycast, single RenderStepped for aimbot, proper connections, unique config flags.

local ESP = loadstring(game:HttpGet("https://raw.githubusercontent.com/linemaster2/esp-library/main/library.lua"))()

ESP.Enabled = false
ESP.ShowBox = false
ESP.ShowName = false
ESP.ShowHealth = false
ESP.ShowTracer = false
ESP.ShowDistance = false
ESP.ShowSkeletons = false

local aimbotEnabled = false
local aimAtPart = "HumanoidRootPart"
local wallCheckEnabled = false
local targetNPCs = false
local teamCheckEnabled = false
local headSizeEnabled = false

-- Services
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local localPlayer = Players.LocalPlayer

-- Character handler (updates on respawn)
local character = localPlayer.Character or localPlayer.CharacterAdded:Wait()
localPlayer.CharacterAdded:Connect(function(newChar)
	character = newChar
end)

-- Get closest target (fixed raycast + safety checks)
local function getClosestTarget()
	local Cam = workspace.CurrentCamera
	local localRoot = character:FindFirstChild("HumanoidRootPart")
	if not localRoot then return nil end

	local nearestTarget = nil
	local shortestDistance = math.huge

	local function checkTarget(target)
		if not target or not target:IsA("Model") or not target:FindFirstChild("Humanoid") or not target:FindFirstChild(aimAtPart) then return end
		if target.Humanoid.Health <= 0 then return end

		local targetRoot = target[aimAtPart]
		local distance = (targetRoot.Position - localRoot.Position).Magnitude

		if distance >= shortestDistance then return end

		local canSee = true
		if wallCheckEnabled then
			local rayDirection = (targetRoot.Position - Cam.CFrame.Position) * (distance + 10)
			local raycastParams = RaycastParams.new()
			raycastParams.FilterDescendantsInstances = {character}
			raycastParams.FilterType = Enum.RaycastFilterType.Exclude

			local raycastResult = workspace:Raycast(Cam.CFrame.Position, rayDirection, raycastParams)
			if raycastResult and not raycastResult.Instance:IsDescendantOf(target) then
				canSee = false
			end
		end

		if canSee then
			shortestDistance = distance
			nearestTarget = target
		end
	end

	-- Players
	for _, player in pairs(Players:GetPlayers()) do
		if player ~= localPlayer and (not teamCheckEnabled or player.Team ~= localPlayer.Team) and player.Character then
			checkTarget(player.Character)
		end
	end

	-- NPCs (if enabled)
	if targetNPCs then
		for _, obj in pairs(workspace:GetDescendants()) do
			if obj:IsA("Model") then
				checkTarget(obj)
			end
		end
	end

	return nearestTarget
end

-- Camera aim
local function lookAt(targetPosition)
	local Cam = workspace.CurrentCamera
	if targetPosition then
		Cam.CFrame = CFrame.new(Cam.CFrame.Position, targetPosition)
	end
end

-- Aimbot (single efficient RenderStepped - no laggy while loops)
local aimConnection = nil
local function toggleAimbot(enabled)
	aimbotEnabled = enabled
	if enabled then
		if aimConnection then aimConnection:Disconnect() end
		aimConnection = RunService.RenderStepped:Connect(function()
			if not aimbotEnabled then
				if aimConnection then aimConnection:Disconnect() end
				aimConnection = nil
				return
			end

			local closestTarget = getClosestTarget()
			if closestTarget and closestTarget:FindFirstChild(aimAtPart) then
				lookAt(closestTarget[aimAtPart].Position)
			end
		end)
	else
		if aimConnection then
			aimConnection:Disconnect()
			aimConnection = nil
		end
	end
end

-- Head size (hitbox expander)
local function resizeHeads()
	for _, player in pairs(Players:GetPlayers()) do
		if player ~= localPlayer and player.Character then
			local head = player.Character:FindFirstChild("Head")
			if head and head:IsA("BasePart") then
				head.Size = Vector3.new(5, 5, 5)
				head.CanCollide = false
			end
		end
	end
	for _, npc in pairs(workspace:GetDescendants()) do
		if npc:IsA("Model") then
			local head = npc:FindFirstChild("Head")
			if head and head:IsA("BasePart") then
				head.Size = Vector3.new(5, 5, 5)
				head.CanCollide = false
			end
		end
	end
end

-- Infinite Jump setup
local IJ = false
local jumpConnection = nil
local function setupInfiniteJump()
	if jumpConnection then return end
	jumpConnection = UserInputService.JumpRequest:Connect(function()
		if IJ and localPlayer.Character then
			local hum = localPlayer.Character:FindFirstChildOfClass("Humanoid")
			if hum then
				hum:ChangeState(Enum.HumanoidStateType.Jumping)
			end
		end
	end)
end

-- Speed hack setup
local _GWS = 16
local speedConnection = nil
local function setupSpeedHack()
	if speedConnection then return end
	speedConnection = RunService.Heartbeat:Connect(function()
		if localPlayer.Character then
			local hum = localPlayer.Character:FindFirstChild("Humanoid")
			if hum and hum.WalkSpeed ~= _GWS then
				hum.WalkSpeed = _GWS
			end
		end
	end)
end

-- UI Libraries
local OrionLib = loadstring(game:HttpGet(('https://raw.githubusercontent.com/jensonhirst/Orion/main/source')))()
local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
	Name = "Onetap ReCoded | Rivals",
	Icon = 0,
	LoadingTitle = "Rivals Script",
	LoadingSubtitle = "by Aidar & Smash (fixed by Grok)",
	Theme = "Ocean",
	DisableRayfieldPrompts = false,
	DisableBuildWarnings = false,
	ConfigurationSaving = {
		Enabled = true,
		FolderName = nil,
		FileName = "OnetapRivals"
	},
	Discord = {
		Enabled = false,
		Invite = "noinvitelink",
		RememberJoins = true
	},
	KeySystem = false,
	KeySettings = {
		Title = "Untitled",
		Subtitle = "Key System",
		Note = "No method of obtaining the key is provided",
		FileName = "Key",
		SaveKey = true,
		GrabKeyFromSite = false,
		Key = {"Hello"}
	}
})

-- ==================== AIMBOT TAB ====================
local AimbotTab = Window:CreateTab("Aimbot", 4483362458)
local AimbotSection = AimbotTab:CreateSection("Aimbot Settings")

local SilentButton = AimbotTab:CreateButton({
	Name = "Silent Aim by Bolts (Rivals)",
	Callback = function()
		loadstring(game:HttpGet('https://raw.githubusercontent.com/ThunderScriptSolutions/Misc/refs/heads/main/RivalsSilentAim'))()
	end,
})

local AimbotToggle = AimbotTab:CreateToggle({
	Name = "Camera Aimbot",
	CurrentValue = false,
	Flag = "AimbotToggle",
	Callback = function(Value)
		toggleAimbot(Value)
	end,
})

local SwitchButton = AimbotTab:CreateButton({
	Name = "Switch Aim Part (Torso ↔ Head)",
	Callback = function()
		aimAtPart = (aimAtPart == "HumanoidRootPart") and "Head" or "HumanoidRootPart"
		OrionLib:MakeNotification({
			Name = "Aim Part Updated",
			Content = "Now aiming at: " .. aimAtPart,
			Image = "rbxassetid://4483345998",
			Time = 5
		})
	end,
})

local WallToggle = AimbotTab:CreateToggle({
	Name = "Wall Check",
	CurrentValue = false,
	Flag = "WallCheckToggle",
	Callback = function(Value)
		wallCheckEnabled = Value
	end,
})

local TeamToggle = AimbotTab:CreateToggle({
	Name = "Team Check",
	CurrentValue = false,
	Flag = "TeamCheckToggle",
	Callback = function(Value)
		teamCheckEnabled = Value
	end,
})

local NPCToggle = AimbotTab:CreateToggle({
	Name = "Target NPCs",
	CurrentValue = false,
	Flag = "NPCToggle",
	Callback = function(Value)
		targetNPCs = Value
	end,
})

local HeadSizeToggle = AimbotTab:CreateToggle({
	Name = "Head Size Expander (Hitbox)",
	CurrentValue = false,
	Flag = "HeadSizeToggle",
	Callback = function(Value)
		headSizeEnabled = Value
		if Value then resizeHeads() end
	end,
})

-- ==================== ESP TAB ====================
local ESPTab = Window:CreateTab("ESP | WALLHACK", "rewind")
local ESPSection = ESPTab:CreateSection("ESP Settings")

local EnableESPToggle = ESPTab:CreateToggle({
	Name = "Enable ESP",
	CurrentValue = false,
	Flag = "EnableESPToggle",
	Callback = function(Value)
		ESP.Enabled = Value
	end,
})

local BoxToggle = ESPTab:CreateToggle({
	Name = "ESP Box",
	CurrentValue = false,
	Flag = "BoxToggle",
	Callback = function(Value)
		ESP.ShowBox = Value
	end,
})

local NameToggle = ESPTab:CreateToggle({
	Name = "ESP Name",
	CurrentValue = false,
	Flag = "NameToggle",
	Callback = function(Value)
		ESP.ShowName = Value
	end,
})

local TracerToggle = ESPTab:CreateToggle({
	Name = "ESP Tracer",
	CurrentValue = false,
	Flag = "TracerToggle",
	Callback = function(Value)
		ESP.ShowTracer = Value
	end,
})

local DistanceToggle = ESPTab:CreateToggle({
	Name = "ESP Distance",
	CurrentValue = false,
	Flag = "DistanceToggle",
	Callback = function(Value)
		ESP.ShowDistance = Value
	end,
})

local SkeletonToggle = ESPTab:CreateToggle({
	Name = "ESP Skeleton",
	CurrentValue = false,
	Flag = "SkeletonToggle",
	Callback = function(Value)
		ESP.ShowSkeletons = Value
	end,
})

local TeamCheckESPToggle = ESPTab:CreateToggle({
	Name = "ESP Team Check",
	CurrentValue = false,
	Flag = "TeamCheckESPToggle",
	Callback = function(Value)
		ESP.TeamCheck = Value
	end,
})

local BoxTypeDropdown = ESPTab:CreateDropdown({
	Name = "ESP Box Type",
	Options = {"2D", "Corner Box Esp"},
	CurrentOption = {"2D"},
	MultipleOptions = false,
	Flag = "BoxTypeDropdown",
	Callback = function(Value)
		ESP.BoxType = Value -- Rayfield passes string for single select
	end,
})

local TracerPosDropdown = ESPTab:CreateDropdown({
	Name = "Tracer Position",
	Options = {"Bottom", "Top", "Middle"},
	CurrentOption = {"Top"},
	MultipleOptions = false,
	Flag = "TracerPosDropdown",
	Callback = function(Value)
		ESP.TracerPosition = Value
	end,
})

-- ==================== MISC TAB ====================
local MiscTab = Window:CreateTab("Misc", 4483362458)
local MiscSection = MiscTab:CreateSection("Misc Features")

local IJToggle = MiscTab:CreateToggle({
	Name = "Infinite Jump",
	CurrentValue = false,
	Flag = "IJToggle",
	Callback = function(Value)
		IJ = Value
	end,
})

local SpeedSlider = MiscTab:CreateSlider({
	Name = "Speed Hack",
	Range = {0, 100},
	Increment = 1,
	Suffix = "Studs",
	CurrentValue = 16,
	Flag = "SpeedSlider",
	Callback = function(Value)
		_GWS = Value
		-- Instant apply if alive
		if localPlayer.Character and localPlayer.Character:FindFirstChild("Humanoid") then
			localPlayer.Character.Humanoid.WalkSpeed = _GWS
		end
	end,
})

-- ==================== INFO TAB ====================
local InfoTab = Window:CreateTab("Info | Authors", 4483362458)
local InfoSection = InfoTab:CreateSection("Credits")

local Label1 = InfoTab:CreateLabel("Base by Aidar & Smash", 4483362458, Color3.fromRGB(255, 255, 255), false)
local Label2 = InfoTab:CreateLabel("Rayfield UI • Fixed & Optimized for Rivals", 4483362458, Color3.fromRGB(255, 255, 255), false)
local Label3 = InfoTab:CreateLabel("Silent Aim from ThunderScript (Rivals specific)", 4483362458, Color3.fromRGB(255, 255, 255), false)

-- Initialize permanent connections
setupInfiniteJump()
setupSpeedHack()

print("✅ Onetap ReCoded | Rivals loaded successfully. Use the GUI tabs above.")
